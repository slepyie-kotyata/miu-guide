services:
    backend:
        build: ./backend
        ports:
            - "8080":"8080"
        env_file: .env
        volumes:
            - ./backend/storage:/app/db
        environment:
            - SQLITE_PATH=/app/db/local.db
        